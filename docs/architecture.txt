Best Folder Structure for Your Vanilla JS Portfolio App
Here’s a clean, simple, and scalable structure focused on separating concerns — which is one of the most important habits senior engineers follow.
Project Structure (Visual Representation)
Bashportfolio-app/
├── index.html
├── css/
│   └── styles.css
├── js/
│   ├── main.js                 # App entry point (initialization)
│   ├── config.js               # Constants & configuration
│   ├── utils/
│   │   └── helpers.js          # General reusable utilities
│   ├── data/
│   │   ├── storage.js          # localStorage CRUD operations
│   │   └── model.js            # Data structures / schema
│   ├── business/
│   │   └── portfolioLogic.js   # Core business rules & processing
│   ├── ui/
│   │   ├── renderer.js         # Functions that build/update DOM
│   │   ├── formHandler.js      # Form input handling & validation
│   │   └── eventListeners.js   # Attaching clicks, submits, etc.
│   └── components/             # Optional: reusable UI pieces
│       └── card.js
└── README.md
Detailed Explanation of Each Folder/File



Detailed Explanation of Each Folder/File


Decision: When deciding on how to store the data locally for this app I considered localStorage versus IndexedDB. For the purpose of this app, I decided that localStorage was the simplist choice here because the data will be below the 5M limit and at this point in the app's maturity it is sufficient. I will consider using IndexedDB and MongoDB in future advanced versions.

Decision: When creating the user onboarding form, the first priority was to reduce the risk of malicious code being executed in the browser. I chose a safer rendering approach by creating UI elements programmatically rather than just injecting raw HTML. 

const form = document.getElementById('userForm');
const output = document.getElementById('output');

form.addEventListener('submit', (e) => {
  e.preventDefault(); // stop the page from reloading

  const name = document.getElementById('nameInput').value;
  const message = document.getElementById('messageInput').value;

  // Build the display safely
  displayUserEntry(name, message);

  form.reset();
});

function displayUserEntry(name, message) {
  const entry = document.createElement('div');
  entry.classList.add('entry');

  const nameEl = document.createElement('strong');
  nameEl.textContent = name; // textContent = safe, treated as plain text

  const messageEl = document.createElement('p');
  messageEl.textContent = message; // same here

  entry.appendChild(nameEl);
  entry.appendChild(messageEl);
  output.appendChild(entry);
}


FROM CLAUDE:
function sanitize(input) {
  // Create a temporary, detached element that's never added to the page.
  // This means anything we put inside it can NEVER execute —
  // it's like a quarantine room, not the actual house.
  const tempDiv = document.createElement('div');

  // Setting textContent (not innerHTML) tells the browser:
  // "Treat this as literal text, not markup."
  tempDiv.textContent = input;

  // Reading innerHTML back out now gives us the ESCAPED version —
  // e.g. "<script>" becomes "&lt;script&gt;" — safe to display or store.
  return tempDiv.innerHTML;
}


document.getElementById('messageForm').addEventListener('submit', function (e) {
  e.preventDefault(); // stop the page from reloading

  const nameInput = document.getElementById('nameField').value;
  const messageInput = document.getElementById('messageField').value;

  // Run both fields through our bouncer before doing anything else with them.
  const safeName = sanitize(nameInput);
  const safeMessage = sanitize(messageInput);

  // Build the display entry
  const entry = document.createElement('div');
  entry.classList.add('entry');

  // IMPORTANT: even though safeName/safeMessage are sanitized,
  // we still use textContent here (not innerHTML) as a second layer of safety.
  // Belt and suspenders — never trust user data twice in a row with innerHTML.
  const nameEl = document.createElement('strong');
  nameEl.textContent = safeName || 'Anonymous';

  const messageEl = document.createElement('p');
  messageEl.textContent = safeMessage;

  entry.appendChild(nameEl);
  entry.appendChild(messageEl);

  document.getElementById('output').prepend(entry);

  // Clear the form for the next entry
  this.reset();
});



  <form id="messageForm">
    <label for="nameField">Name:</label>
    <input type="text" id="nameField" placeholder="Your name">

    <label for="messageField">Message:</label>
    <textarea id="messageField" rows="4" placeholder="Say something..."></textarea>

    <button type="submit">Submit</button>
  </form>

  <div id="output"></div>


    input, textarea { width: 100%; padding: 8px; margin-bottom: 10px; box-sizing: border-box; }
    #output { border: 1px solid #ccc; padding: 10px; margin-top: 20px; border-radius: 6px; }
    .entry { border-bottom: 1px solid #eee; padding: 6px 0; }
































































Folder / File                 PurposeWhy It’s Needed
js/main.js
Bootstraps the app: loads data, sets up event listeners, renders initial UISingle entry point makes it easy to understand how the app starts

js/config.js
Stores constants (keys for localStorage, default values, app settings)Avoids magic strings scattered everywhere; easy to change later

js/utils/helpers.js
Small reusable functions (date formatting, ID generation, DOM helpers, validation)
Prevents code duplication and keeps other files cleaner

js/data/Data persistence layer
Separates how data is saved/read from what the app does with it

→ storage.js
All localStorage read/write operations (save, load, delete, clear) Isolates browser storage logic. Easy to swap to IndexedDB or backend later

→ model.js
Defines data shape (e.g. Portfolio Item object structure, defaults)
Acts as a contract for what valid data looks likejs/business/Core business logic — this is the heart of your app. Keeps decision-making and rules separate from UI and storage

→ portfolioLogic.js
Functions like addEntry(), calculateTotal(), filterByCategory(), validateEntry(), deleteEntry() etc.Contains all the “rules” of your portfolio (how data is processed, validated, computed). This is where most of your thinking lives.

js/ui/ Everything related to the user interface and interaction
Keeps visual concerns separate from business rules

→ renderer.js
Functions that generate HTML and update the DOM (renderPortfolio(), renderStats())
All DOM manipulation lives here

→ formHandler.js
Handles reading form inputs, basic validation, and preparing data for business layerCleans and prepares user input before it reaches business logic

→ eventListeners.js
Attaches all click, submit, input events and wires them together
Keeps main.js from becoming too clutteredjs/components/Reusable UI components (e.g. portfolio item card)Good practice even in vanilla JS for larger apps























Folder / FilePurposeWhy It’s Neededjs/main.jsBootstraps the app: loads data, sets up event listeners, renders initial UISingle entry point makes it easy to understand how the app startsjs/config.jsStores constants (keys for localStorage, default values, app settings)Avoids magic strings scattered everywhere; easy to change laterjs/utils/helpers.jsSmall reusable functions (date formatting, ID generation, DOM helpers, validation)Prevents code duplication and keeps other files cleanerjs/data/Data persistence layerSeparates how data is saved/read from what the app does with it  → storage.jsAll localStorage read/write operations (save, load, delete, clear)Isolates browser storage logic. Easy to swap to IndexedDB or backend later  → model.jsDefines data shape (e.g. PortfolioItem object structure, defaults)Acts as a contract for what valid data looks likejs/business/Core business logic — this is the heart of your appKeeps decision-making and rules separate from UI and storage  → portfolioLogic.jsFunctions like addEntry(), calculateTotal(), filterByCategory(), validateEntry(), deleteEntry() etc.Contains all the “rules” of your portfolio (how data is processed, validated, computed). This is where most of your thinking lives.js/ui/Everything related to the user interface and interactionKeeps visual concerns separate from business rules  → renderer.jsFunctions that generate HTML and update the DOM (renderPortfolio(), renderStats())All DOM manipulation lives here  → formHandler.jsHandles reading form inputs, basic validation, and preparing data for business layerCleans and prepares user input before it reaches business logic  → eventListeners.jsAttaches all click, submit, input events and wires them togetherKeeps main.js from becoming too clutteredjs/components/Reusable UI components (e.g. portfolio item card)Good practice even in vanilla JS for larger apps
Why This Structure Works Well

Separation of Concerns — UI, Business Logic, and Data layers are clearly divided. This makes debugging and adding features much easier.
Maintainability — You know exactly where to go:
Changing how data is saved? → data/storage.js
Adding new business rules? → business/portfolioLogic.js
Updating how things look? → ui/renderer.js

Scalability — As your portfolio grows (adding charts, export features, multiple sections), this structure handles it without becoming messy.
Testability — Business logic in portfolioLogic.js can be tested independently of the DOM or localStorage.
Simple enough for Vanilla JS — Not over-engineered, but professional.

Quick Usage Example
In main.js:
JavaScriptimport { loadPortfolio } from './data/storage.js';
import { renderPortfolio } from './ui/renderer.js';
import { addNewEntry } from './business/portfolioLogic.js';

const data = loadPortfolio();
renderPortfolio(data);

// When user submits form:
const newEntry = formHandler.getFormData();
const validated = portfolioLogic.validateEntry(newEntry);
if (validated) {
    const updated = portfolioLogic.addNewEntry(data, validated);
    storage.savePortfolio(updated);
    renderer.renderPortfolio(updated);
}