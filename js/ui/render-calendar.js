// Renders the weekly calendar to the UI

export function render_UI() {
  const uiContainer = document.createElement("div");
  uiContainer.className = "test";
  document.getElementById("main-container").appendChild(uiContainer);
}
render_UI();
